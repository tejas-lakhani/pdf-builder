{
 "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      },

      "env": {
      "NPM_CONFIG_LEGACY_PEER_DEPS": "true"
    }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}